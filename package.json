{
  "name": "accessible-table-ocr-check",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && node scripts/postbuild.mjs",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run tests/*.test.ts && playwright test",
    "test:unit": "vitest run tests/*.test.ts",
    "test:e2e": "playwright test",
    "test:claims": "vitest run tests/*.test.ts --testNamePattern @claim: && playwright test --project=chromium --grep @claim:",
    "test:live-rate-limit": "node scripts/verify-live-rate-limit.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.10.2",
    "@eslint/js": "10.0.1",
    "@playwright/test": "1.58.2",
    "@types/node": "24.3.0",
    "eslint": "10.9.1",
    "globals": "17.11.0",
    "typescript": "5.9.2",
    "typescript-eslint": "8.68.0",
    "vite": "7.3.6",
    "vitest": "3.2.7"
  },
  "overrides": {
    "playwright-core": "1.58.2"
  }
}
